[build]
rustflags = [
    "--cfg",
    "place_addr",
    "--cfg",
    "info_db_fmt=\"rkyv\"",
]

[env]
# You can set this (during compile time) to change the name of the runtime library
# in the extern prelude of the compiled program.
