# Configuration

Leaf's configuration is organized by the component it controls. The compiler
configuration reference describes `leafc` settings, including pass selection,
instrumentation rules, runtime-shim configuration, and configuration loading.

- [Compiler configuration](./configuration/compiler.md)
