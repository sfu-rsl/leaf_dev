[build]
rustflags = [
    "--cfg",
    "runtime_access=\"safe_brt\"",
    "--cfg",
    "place_addr",
    "--cfg",
    "abs_concrete",
    $core_build_cfg_switch "--cfg",
    $core_build_cfg_switch "core_build",
    "-Zalways-encode-mir",
    "-Ccodegen-units=1",
]

[env]
LEAFC_RUNTIME_SHIM__AS_EXTERNAL = "false"
LEAFC_BUILDING_CORE = "true"
